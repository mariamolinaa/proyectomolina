import { CartWidget } from "./CartWidget";


function Navbar() {
  return (
    <header>
      <button>Home</button>
      <p>Nosotros</p>
      <a src="#"> Tienda</a>
      <CartWidget/>
      
    </header>

  );
}
export default Navbar; 